extends /base.jade
block vars
  - var navbar = false
  - var authPanelTitle = "Can't Let You In.";
  - var authPanelDesc = "Try a different method to login";
block head
  +css("/css/site/index.css")
block body
  .root-bk-gradient(ng-controller="authPage"): .inner
    .authpanel-static-outer(ng-show="!user.data.key")
      include ../static.jade
    .text-center(ng-show="user.data.key")
      +nbr(4)
      h2 redirecting...
      +nbr(4)
    +nbr(1)
    .text-center: a(href="/") Back to Home
